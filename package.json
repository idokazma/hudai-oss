{
  "name": "hudai",
  "version": "0.1.0",
  "packageManager": "npm@11.3.0",
  "description": "A command center for AI code agents",
  "author": "Ido Kazma",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/idokazma/hudai-oss.git"
  },
  "homepage": "https://github.com/idokazma/hudai-oss",
  "bugs": "https://github.com/idokazma/hudai-oss/issues",
  "bin": {
    "hudai": "./packages/server/bin/hudai.js"
  },
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client",
    "packages/telegram-bot"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:server": "npm run dev -w @hudai/server",
    "dev:client": "npm run dev -w @hudai/client",
    "build:shared": "npm run build -w @hudai/shared",
    "dev:telegram": "npm run dev -w @hudai/telegram-bot",
    "build:bundle": "npm run build -w @hudai/shared && npm run build -w @hudai/server && npm run build -w @hudai/client && mkdir -p packages/server/public && cp -r packages/client/dist/* packages/server/public/",
    "start": "npm run build:bundle && node packages/server/bin/hudai.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^27.0.1",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.4"
  }
}
